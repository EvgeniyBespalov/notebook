<%= form_for @note do |f|%>
<table class="tableNote">
  <tr>
	 <th colspan="2"><%=@operation_description%></th>
  </tr>
  <tr>
    <td>Тема</td>
    <td><%= f.text_field :header, class: 'textWidth100' %></td>
  </tr>
  <tr>
    <td>Текст</td>
    <td><%= f.text_area :text, class: 'textWidth100' %></td>
  </tr>
	<tr>
    <td>Метки</td>
    <td>
      <% @note_tags.each do |nt|  %>
        <%= check_box_tag "note_tags[]", nt.id, @note.note_tags.exists?(nt.id) %>
        <%= nt.description  %>
      <% end %>
    </td>
  </tr>  
 </table>
 <%-#= f.submit "Сохранить",:controller => 'notes', :action => 'update', :method => :put -%>
 <%= f.submit "Сохранить" %>
<%end%>


